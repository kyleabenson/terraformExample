---
- name: Deploy terraform project
  hosts: localhost
  tasks:
  - name: Build terraform deployment
    shell: terraform plan
    args:
      chdir: './project/'

  - name: Deployment
    terraform:
      project_path: './project/'
      state: present
